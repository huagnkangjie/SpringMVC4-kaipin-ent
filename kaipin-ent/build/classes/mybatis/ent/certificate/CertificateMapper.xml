<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kaipin.enterprise.repository.dao.certificate.ICertificateDao" >
 
	<!-- 获取企业资质文档 -->
	<select id="getEntDoc" resultType="map" parameterType="map">
		select * from company_user_document where ent_user_id = #{userId}
	</select>
</mapper>